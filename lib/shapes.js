//exports the 'triangle', 'circle', and 'square' classes
class Shape {
    constructor(text, shape, color ) {
    this.text = text;
    this.shape = shape;
    this.color = color;
    console.log('Shape');
    }
}

const circle = new Shape(`${text}`, circle, `${color}`); 
const square = new Shape(`${text}`, square, `${color}`); 
const triangle = new Shape(`${text}`, triangle, `${color}`); 
    //   export logo.svg 